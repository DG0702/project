{{>layouts/mypageheader}}
<div class="container">
    <div class="content">
        <div class="content_header">
            <h2>댓글 수정</h2>
            <p>
                게시물에 작성한 댓글만 제공됩니다
            </p>
            <p>
                부적절한 댓글 발견시 즉시 삭제 될 수 있습니다!!
            </p>
        </div>

        <div class="section">
            <form class="comment" id="comment-form">
                <fieldset>
                    <div class="section_private shadow p-3 mb-5 bg-body-tertiary rounded">

                        <div class="userNickname">
                            작성자 : {{comment.userNickname}}
                        </div>

                        <div class="post-title">
                            제목 : {{post.title}}
                        </div>

                        <div class="post-content">
                            내용 : {{post.contents}}

                            <!-- 기존 첨부파일 -->
                            {{#hasAttachedFiles}}
                                <div class="attachment my-4">
                                    <!--이미지-->
                                    {{#images}}
                                        <div class="image mb-3">
                                            <img src="/uploads/{{.}}" alt="이미지 첨부파일" class="img-fluid rounded border border-light p-2" style="max-width: 70%">
                                        </div>
                                    {{/images}}

                                    <!--비디오-->
                                    {{#videos}}
                                        <div class="video mb-3">
                                            <video controls class="img-fluid rounded border border-light p-2" style="max-width: 70%">
                                                <source src="/uploads/{{.}}" type="video/mp4">
                                                <!--브라우저가 지원하지 않을 경우-->
                                                Your browser does not support the video tag.
                                            </video>
                                        </div>
                                    {{/videos}}

                                    <!--문서-->
                                    {{#documents}}
                                        <div class="document mb-3">
                                            <a href="/uploads/{{.}}" class="btn btn-primary" role="button" style="min-width: 120px">문서 다운로드</a>
                                        </div>
                                    {{/documents}}

                                    <!--파일 타입을 알 수 없을 경우(설정하지 않은 파일일 경우)-->
                                    {{#unknowns}}
                                        <div class="unknown mb-3">
                                            <span class="text-danger fw-bold">알 수 없는 파일</span>
                                        </div>
                                    {{/unknowns}}
                                </div>
                            {{/hasAttachedFiles}}
                        </div>

                        <div class="comment-content">
                            <input type="hidden" name="commentId" id="commentId" value="{{comment.commentId}}">
                            <input type="text" class="input_comment form-control" name="comment" value="{{comment.comment}}" >
                        </div>

                        <div class="comment-edit-btn">
                            <button type="submit" class="btn btn-outline-primary">수정</button>
                        </div>
                    </div>
                </fieldset>
            </form>
        </div>
{{>layouts/mypagefooter}}