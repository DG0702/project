{{>layouts/mypageheader}}

<div class="main">
    <div class="container">
        <div class="content-header">
            <h3>내가 쓴 게시물</h3>
            <p>
                ※경고※ <br>
                불건전한 게시물, 댓글 발견시 즉시 삭제 될 수 있습니다 !!
            </p>
        </div>

        <table class="table table-light table-striped table-hover">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">게시물 번호</th>
                <th scope="col">작성자</th>
                <th scope="col">제목</th>
                <th scope="col">작성일</th>
                <th scope="col">조회</th>
                <th scope="col">좋아요</th>
                <th scope="col">댓글</th>
            </tr>
            </thead>
            <tbody>
            {{#boardWithInfos}}
            <tr>
                <th scope="row">
                    <input class="form-check-input" type="checkbox" name="check-num" value="{{board.boardId}}">
                </th>
                <td>{{board.boardId}}</td>
                <td>{{board.userNickname}}</td>
                <td>
                    <a href="/posts/{{board.boardId}}" class="text-decoration-none text-black post-link">{{board.title}}</a>
                </td>
                <td>{{board.formattedCreateTime}}</td>
                <td>{{board.viewCount}}</td>
                <td>{{likesCount}}</td>
                <td>{{commentCount}}</td>
            </tr>
            {{/boardWithInfos}}
            </tbody>
        </table>

        <div class="total-check-box">
            <div class="total form-check">
                <input class="form-check-input" type="checkbox" name="total-check" value="total-check"
                       id="flexCheckDefault">
                <label class="form-check-label" for="flexCheckDefault">
                    전체선택
                </label>
            </div>
            <div class="check-btn">
                <div class="edit-btn">
                    <a class="text-decoration-none text-black">
                        <button type="submit" class="btn btn-outline-warning" id="post-edit-btn">수정</button>
                    </a>

                </div>
                <div class="del-btn">
                    <a class="text-decoration-none text-white">
                        <button type="submit" class="btn btn-danger" id="post-delete-btn">삭제</button>
                    </a>
                </div>
            </div>
        </div>


        <!--페이지 번호 -->
        <nav aria-label="Page navigation example">
            <ul class="pagination justify-content-center">
                {{#hasPrevious}}
                <li class="page-item">
                    <a class="page-link text-black" href="/users/{userId}/posts?page={{previousPage}}" aria-label="Previous">
                        <span aria-hidden="true">&lt;이전</span>
                    </a>
                </li>
                {{/hasPrevious}}

                {{#pages}}
                <li class="page-item">
                    <a class="page-link text-black link-primary" href="/users/{userId}/posts?page={{this}}">{{this}}</a>
                </li>
                {{/pages}}

                {{#hasNext}}
                <li class="page-item">
                    <a class="page-link text-black" href="/users/{userId}/posts?page={{nextPage}}" aria-label="Next">
                        <span aria-hidden="true">다음&gt;</span>
                    </a>
                </li>
                {{/hasNext}}
            </ul>
        </nav>



    </div>
</div>

{{>layouts/mypagefooter}}