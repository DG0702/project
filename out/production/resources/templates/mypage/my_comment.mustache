{{>layouts/mypageheader}}

<div class="main">
    <div class="container">
        <div class="content-header">
            <h3>내가 쓴 댓글</h3>
            <p>
                이 목록에서는 게시글에 작성한 댓글만 제공합니다
            </p>

            <p>
                ※경고※ <br>
                불건전한 게시물, 댓글 발견시 즉시 삭제 될 수 있습니다 !!
            </p>
        </div>

        <table class="table table-light table-striped table-hover">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">게시물 번호</th>
                <th scope="col">작성자</th>
                <th scope="col">제목</th>
                <th scope="col">댓글 내용</th>
                <th scope="col">작성일</th>
            </tr>
            </thead>
            <tbody>
            {{#commentWithInfos}}
            <tr>
                <th scope="row">
                    <input class="form-check-input" type="checkbox" name="check-num" value="{{comment.commentId}}">
                </th>
                <td>{{board.boardId}}</td>
                <td>{{comment.userNickname}}</td>
                <td>
                    <a href="/posts/{{board.boardId}}" class="text-decoration-none text-black">{{board.title}}</a>
                </td>
                <td>{{comment.comment}}</td>
                <td>{{comment.formattedCreateTime}}</td>

            </tr>
            {{/commentWithInfos}}
            </tbody>
        </table>

        <div class="total-check-box">
            <div class="total form-check">
                <input class="form-check-input" type="checkbox" name="total-check" value="total-check"
                       id="flexCheckDefault">
                <label class="form-check-label" for="flexCheckDefault">
                    전체선택
                </label>
            </div>
            <div class="check-btn">
                <div class="edit-btn">
                    <a class="text-decoration-none text-black">
                        <button type="submit" class="btn btn-outline-warning" id="comment-edit-btn">수정</button>
                    </a>
                </div>
                <div class="del-btn">
                    <a class="text-decoration-none text-white">
                        <button type="submit" class="btn btn-danger" id="comment-delete-btn">삭제</button>
                    </a>
                </div>
            </div>
        </div>

        <!--페이지 번호-->
        <nav aria-label="Page navigation example">
            <ul class="pagination justify-content-center">
                {{#hasPrevious}}
                <li class="page-item">
                    <a class="page-link text-black" href="/users/{userId}/comments?page={{previousPage}}" aria-label="Previous">
                        <span aria-hidden="true">&lt;이전</span>
                    </a>
                </li>
                {{/hasPrevious}}

                {{#pages}}
                <li class="page-item">
                    <a class="page-link text-black link-primary" href="/users/{userId}/comments?page={{this}}">{{this}}</a>
                </li>
                {{/pages}}

                {{#hasNext}}
                <li class="page-item">
                    <a class="page-link text-black" href="/users/{userId}/comments?page={{nextPage}}" aria-label="Next">
                        <span aria-hidden="true">다음&gt;</span>
                    </a>
                </li>
                {{/hasNext}}
            </ul>
        </nav>

    </div>
</div>


{{>layouts/mypagefooter}}
